<UserControl x:Class="IronBoard.Core.Wpf.PendingChangesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="600">
    <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
       
       <StackPanel Orientation="Horizontal">
         <Button Content="create pre-commit review" Margin="5" x:Name="PreCommitReview" Click="PreCommitReview_OnClick"/>
      </StackPanel>
       
      <ListBox x:Name="ChangesList" Grid.Row="1" SelectionChanged="ChangesList_OnSelectionChanged">
         <ListBox.ItemTemplate>
            <DataTemplate>
               <CheckBox x:Name="CheckBoxZone" Content="{Binding}"></CheckBox>
            </DataTemplate>
         </ListBox.ItemTemplate>   
      </ListBox>

      <Label x:Name="NoChanges" Content="no changes, start working now!" Grid.Row="1"
             HorizontalAlignment="Center" VerticalAlignment="Center"
             Foreground="Red"/>
   </Grid>
</UserControl>
